# --- Data (auto-downloaded by the baseline notebook; keep out of git) ---
Code/data/*.csv

# --- Python ---
__pycache__/
*.pyc
.ipynb_checkpoints/

# --- LaTeX build artifacts ---
*.aux
*.log
*.out
*.toc
*.lof
*.lot
*.bbl
*.bcf
*.blg
*.run.xml
*.fls
*.fdb_latexmk
*.synctex.gz
*.synctex(busy)
*.nav
*.snm
*.vrb

# --- Compiled output PDFs (rebuilt from source; figure PDFs stay tracked) ---
Disertation_at_Work/main.pdf
Grant/main.pdf
Weekly_Meeting_Report/weekly_meeting.pdf

# --- Editor/OS noise ---
.vscode/
.DS_Store
.claude/

# --- Not published (kept local only) ---
Grant/
Weekly_Meeting_Report/
Personal_Weekly_Update/

# --- jupytext notebook pairs (auto-generated by the Jupyter config's
#     default_jupytext_formats = "ipynb,py:percent"; the .ipynb is the source
#     of truth). Listed by name so the real modules stay tracked. ---
baseline_and_federated_methods.py
baseline_full_scale_accuracy.py
consent_churn_study.py
mlp_amplification.py
